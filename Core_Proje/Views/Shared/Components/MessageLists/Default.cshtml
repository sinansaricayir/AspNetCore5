@using EntityLayer.Concrete



<div class="row ">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Mesaj Listesi</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th> Ad Soyad </th>
                                <th> Mesaj İçeriği </th>
                                <th> Mail Adresi </th>
                                <th> Tarih </th>
                                <th> Durum </th>
                            </tr>
                        </thead>
                        <tbody>
                            @*@foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <img src="@item.User.ImageUrl" alt="image" />
                                        <span class="pl-2">@item.User.Name</span>
                                    </td>
                                    <td> @item.Content.Substring(0,30) ... </td>
                                    <td> @item.User.Mail </td>
                                    <td> @item.Date </td>
                                    <td>
                                        @if (item.Status == true)
                                        {


                                            <div class="badge badge-outline-success">Okundu</div>

                                        }
                                        else
                                        {
                                            <div class="badge badge-outline-danger">Okunmadı</div>

                                        }
                                    </td>
                                </tr>
                            }*@
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
